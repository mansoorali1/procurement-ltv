# Environment
.env
__pycache__/
*.pyc
.venv/
venv/

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# MLflow (keep mlruns for the app)
# mlruns/  ← do NOT ignore this, app needs it

# DVC internals only — keep actual data files
.dvc/cache
.dvc/tmp

# Large interim files we don't need in deployment
data/processed/master_df.parquet
data/processed/splits/
data/raw/

# Keep these — app needs them
# data/processed/featured/   ← do NOT ignore
# data/processed/cleaned/    ← do NOT ignore
# models/                    ← do NOT ignore